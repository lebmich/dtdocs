---
author: people
draft: 'false'
id: culling
title: sélection
weight: 30
---

La disposition sélection permet d'afficher les images côte à côte pour les comparer facilement. 

# contrôles

zoom
: En mode de sélection, vous pouvez zoomer les images (jusqu'à 100%) en maintenant la touche Ctrl enfoncée tout en faisant défiler avec la molette de la souris.
: Effectuez un panoramique dans les images zoomées avec un clic+glisser.
: Par défaut, zoomer et faire un panoramique sont synchronisés entre toutes les images visibles. Si vous souhaitez zoomer ou faire un panoramique uniquement sur une image spécifique, ajoutez la touche Maj aux actions ci-dessus.

navigate
: Use the mouse wheel or arrow keys (←/→) to scroll through your collection.

# modes

There are two different ways to define how many images are shown at the same time: “fixed” and “dynamic”. Switch between these methods while in culling mode by pressing the "<" key.

mode fixé
: Le nombre d'images affichées est toujours le même, indépendamment de la longueur de la sélection. Ce nombre peut être défini avec un curseur sur le panneau inférieur.

: Dans ce mode, vous naviguerez dans toutes les images sélectionnées. Si aucune sélection n'est définie (ou si une seule image est sélectionnée), vous parcourrez toutes les images.

: Le raccourci clavier par défaut pour entrer dans la disposition sélection en mode fixe est X .

mode dynamique
: Toutes les images sélectionnées sont affichées. Si aucune sélection n'est définie (ou si une seule image est sélectionnée), la dernière valeur du mode fixe est utilisée.

: Le raccourci clavier par défaut pour entrer dans la disposition sélection en mode dynamique est Ctrl + X .

---

**Conseil :** Pour améliorer les performances lors du chargement d'images zoomées, vous pouvez activer ([préférences > cpu/gpu/mémoire > le disque dur comme cache de prévisualisation](../../preferences-settings/cpu-gpu-memory.md)). Gardez à l'esprit que cela pourrait prendre beaucoup d'espace disque .

---
